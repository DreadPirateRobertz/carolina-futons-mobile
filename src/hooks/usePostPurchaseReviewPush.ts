/**
 * @module usePostPurchaseReviewPush
 *
 * Stub — constants shared with NPSSurveyBridge.
 * Full implementation is a separate bead.
 */

/** AsyncStorage prefix for per-order push records. */
export const PUSH_STORAGE_PREFIX = '@cfutons/review_push_';

/** AsyncStorage key for the ordered list of nudged order IDs. */
export const NUDGES_INDEX_KEY = '@cfutons/review_nudges_index';
